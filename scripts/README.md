# scripts/ — Scripts utilitarios

- `setup.sh` — instala dependencias Python, instala o navegador do
  Playwright e cria o arquivo `.env` a partir de `.env.example` (se
  ainda nao existir).

  ```bash
  ./scripts/setup.sh
  ```
