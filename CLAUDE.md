# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository purpose

This is a GitHub **profile README** repository (repo name matches the GitHub username `bilali-dev`), so `README.md` is rendered directly on the user's GitHub profile page. It contains no application code, build system, tests, or dependencies — just the profile Markdown.

## Structure

- `README.md` — the entire content of the repo. Sections: intro/bio, tech stack, a featured project link (external repo: `bilali-dev/python-automation`), and contact/social links.

## Working in this repo

- There are no build, lint, or test commands — changes are edits to `README.md`.
- Preserve the emoji-headed section format (`### 🛠️ ...`) and the `---` horizontal-rule separators between sections when editing.
- The "Featured Project" link points to a separate repository (`python-automation`); it is not part of this codebase.
- Since this file is rendered as a public profile page, keep Markdown changes visually clean (check heading levels, list formatting, and link syntax) since there's no build step to catch rendering issues.
