name: VFS Visa Monitor

on:
  schedule:
    # A cada 15 minutos. O GitHub pode atrasar execuções agendadas em
    # períodos de carga alta — não é garantido ao minuto exato.
    - cron: "*/15 * * * *"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: vfs-monitor
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        working-directory: vfs_monitor
        run: pip install -r requirements.txt

      - name: Run single monitor pass
        working-directory: vfs_monitor
        env:
          VFS_URL: ${{  https://visa.vfsglobal.com/gnb/pt/prt/login }}
          VFS_EMAIL: ${{ vladmirgomessoares@gmail.com }}
          VFS_PASSWORD: ${{ Bilall1820@ }}
          VISA_CATEGORY_TEXTS: ${{ Visto para Tratamento Médico }}
          TELEGRAM_BOT_TOKEN: ${{ 8967038160:AAFgxZwLRzqBjtfMVd6rkI0MXpLi_smEKfg }}
          TELEGRAM_CHAT_ID: ${{ 8763356256 }}
          ALERT_COOLDOWN_SECONDS: ${{ VAGA ABERTO }}
          HEADLESS: "true"
        run: python vfs_monitor.py --once

      - name: Persist alert-cooldown state
        run: |
          git config user.name "vfs-monitor-bot"
          git config user.email "actions@users.noreply.github.com"
          git add vfs_monitor/state.json
          git diff --cached --quiet || git commit -m "chore: update monitor state [skip ci]"
          git push
