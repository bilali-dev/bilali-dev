[project]
name = "market-seller-api"
version = "0.1.0"
description = "Extract structured product and seller data from marketplace pages."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
